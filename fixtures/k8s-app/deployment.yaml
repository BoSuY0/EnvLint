kind: Deployment
apiVersion: apps/v1
metadata:
  name: envlint-example
spec:
  template:
    spec:
      containers:
        - name: api
          image: envlint/example
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db
                  key: url

