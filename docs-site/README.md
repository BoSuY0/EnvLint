# EnvLint Docs Site

The v1.0 repository ships documentation as Markdown under `docs/`. This directory is reserved for a future static docs site build and keeps the package boundary explicit without adding a web framework to the first stable release.

